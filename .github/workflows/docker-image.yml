name: Docker Image CI Phylo

on:
  push:
  workflow_dispatch:
    branches:
      - '*'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag phylo:$(date +%s)
